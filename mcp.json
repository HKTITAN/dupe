{
  "$schema": "https://agent-plugins.org/schemas/1.1.0/mcp.schema.json",
  "mcpServers": {
    "dupe": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/src/mcp.js"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
